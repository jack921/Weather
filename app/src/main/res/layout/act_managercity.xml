<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

        <include
            android:id="@+id/mamage_header"
            layout="@layout/part_toolbar"/>

       <View
           android:id="@+id/act_man_divider"
           android:layout_width="match_parent"
           android:layout_height="0.5dip"
           android:layout_below="@+id/mamage_header"
           android:background="@color/act_divider"/>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/manager_city"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/act_man_divider"
        android:layout_alignParentLeft="true"
        android:layout_marginTop="10dp"
        android:layout_alignParentStart="true" />


</RelativeLayout>